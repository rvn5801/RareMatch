[pytest]
# Project root — makes 'from backend.xxx' imports work in tests
pythonpath = .

# Test discovery
testpaths = tests

# Output
addopts = -v --tb=short

# Markers
markers =
    unit: Pure unit tests — no network, no API keys needed
    integration: Requires live API (Gemini key + network)
    safety: Safety filter specific tests